export interface LoginState {
  username: string;
  password: string;
  social: string;
};

export interface UserState {
  email: string;
  profileName: string;
  imageUrl: string;
  role: string;
};