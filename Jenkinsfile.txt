pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                checkout scmGit(
							    branches: [[name: 'v4.9.x']],
							    userRemoteConfigs: [[credentialsId: 'GITHub_Username_Password',
	        				url: 'https://github.com/me92years/portfolio-project.git']]
        				)
            }
        }
        stage('test') {
            steps {
                echo 'testing the application...'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploying the application...'
            }
        }
    }
}